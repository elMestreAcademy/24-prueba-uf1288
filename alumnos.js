const alumnoss = require('./alumnos.json');

const FILENAME = "alumnos.json";
console.log(alumnos);

console.log=typeof;
console.log =(alumnos.length);
console.log =("nombre:" +alumnos.nombre);
console.log=("edad:" +alumnos.edad);
console.log=("curso:" +alumnos.curso);
console.log=("id:" + alumnos.id);

parametrosalumnos;
utf-8 = [];

contador = 1;
alumnos.forEach((alumnos) => {
  equipo = Object.assign(
    {
      alumno : contador,
      nombre: "nombre" + alumnos,
      id: "" + (4+ ++contador),
      curso: makeid(4),
    },
    parametrosalumnos
  );
  utf-8.push(alumnos);
});
